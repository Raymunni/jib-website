---
// Jib on the web: sign in and manage the same homes, jobs, tools,
// shopping list and Toolbox as the phone app. Reads and writes the same
// users/{uid} document the app syncs, so edits show up on the phone.
import '../../styles/global.css';
import Logo from '../../components/Logo.astro';
import Analytics from '../../components/Analytics.astro';
---

<!doctype html>
<html lang="en-AU">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Jib on the web</title>
    <meta name="description" content="Sign in to manage your Jib jobs, tools, shopping list and Toolbox from your computer." />
    <meta name="robots" content="noindex" />
    <link rel="canonical" href="https://jibapp.xyz/web/" />
    <meta name="theme-color" content="#0e1824" />
    <Analytics />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <link rel="icon" href="/favicon-32.png" sizes="32x32" type="image/png" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
  </head>
  <body class="webapp">
    <header class="w-header">
      <div class="w-top">
        <a class="w-brand" href="/" aria-label="Jib home"><Logo size={34} color="#ffffff" /></a>
        <nav class="w-site" aria-label="Jib website">
          <a href="/">Home</a>
          <a href="/guides/">Guides</a>
          <a href="/guides/features/">Features</a>
          <a href="/tools/">Free tools</a>
          <a href="/guides/home-maintenance/">Maintenance</a>
          <a href="/about/">About</a>
          <a href="/web/" aria-current="page">My Jib</a>
        </nav>
        <div id="w-top-slot" class="w-top-slot"></div>
      </div>
      <div id="w-tabs-bar" class="w-subbar" hidden></div>
    </header>
    <main id="app" class="w-main" aria-live="polite">
      <div class="w-loading"><span class="spinner" aria-hidden="true"></span> Loading…</div>
    </main>
    <div id="w-toast" class="w-toast" role="status" aria-live="polite"></div>
    <script>
      import '../../web/app.ts';
    </script>
  </body>
</html>

<style is:global>
  body.webapp {
    min-height: 100vh;
    background: var(--bg);
  }
  .w-header {
    position: sticky;
    top: 0;
    z-index: 20;
    background: rgba(14, 24, 36, 0.94);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid var(--border);
  }
  .w-top {
    display: flex;
    align-items: center;
    gap: 16px;
    padding: 10px 16px;
  }
  .w-site {
    display: flex;
    gap: 2px;
    overflow-x: auto;
    scrollbar-width: none;
    min-width: 0;
  }
  .w-site a {
    color: var(--dim);
    text-decoration: none;
    font-weight: 600;
    font-size: 0.95rem;
    padding: 6px 10px;
    border-radius: 8px;
    white-space: nowrap;
  }
  .w-site a:hover {
    color: #fff;
    background: var(--surface);
  }
  .w-site a[aria-current='page'] {
    color: #fff;
  }
  .w-subbar {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 6px 16px 8px;
    border-top: 1px solid var(--border);
  }
  .w-subbar[hidden] {
    display: none;
  }
  .w-brand {
    display: flex;
    align-items: center;
    gap: 8px;
    color: #fff;
    text-decoration: none;
    font-weight: 800;
  }
  .w-brand span {
    font-size: 0.8rem;
    color: var(--dim);
    text-transform: uppercase;
    letter-spacing: 0.12em;
  }
  .w-top-slot {
    flex: 1;
    display: flex;
    align-items: center;
    gap: 10px;
    min-width: 0;
  }
  .w-main {
    max-width: 1080px;
    margin: 0 auto;
    padding: 20px 16px 80px;
  }
  .w-loading {
    display: flex;
    gap: 10px;
    align-items: center;
    justify-content: center;
    padding: 80px 0;
    color: var(--dim);
  }
  .spinner {
    width: 18px;
    height: 18px;
    border-radius: 50%;
    border: 2px solid var(--border);
    border-top-color: var(--accent-2);
    animation: spin 0.8s linear infinite;
  }
  @keyframes spin {
    to {
      transform: rotate(360deg);
    }
  }

  /* Tabs */
  .w-tabs {
    display: flex;
    gap: 4px;
    overflow-x: auto;
    scrollbar-width: none;
  }
  .w-tabs button {
    background: none;
    border: 0;
    color: var(--dim);
    font: inherit;
    font-weight: 700;
    padding: 8px 12px;
    border-radius: 999px;
    cursor: pointer;
    white-space: nowrap;
  }
  .w-tabs button[aria-current='page'] {
    background: var(--surface-2);
    color: #fff;
  }
  .w-spacer {
    flex: 1;
  }
  .w-select,
  .w-input,
  .w-textarea {
    font: inherit;
    color: var(--ink);
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    padding: 9px 12px;
    min-width: 0;
  }
  .w-input:focus,
  .w-textarea:focus,
  .w-select:focus {
    outline: 2px solid var(--accent-2);
    outline-offset: 1px;
  }
  .w-textarea {
    width: 100%;
    min-height: 90px;
    resize: vertical;
  }
  .w-btn {
    font: inherit;
    font-weight: 700;
    border-radius: 999px;
    padding: 9px 16px;
    border: 1px solid var(--border);
    background: var(--surface-2);
    color: var(--ink);
    cursor: pointer;
    white-space: nowrap;
  }
  .w-btn:hover {
    border-color: var(--accent-2);
  }
  .w-btn.primary {
    background: var(--accent);
    border-color: var(--accent);
    color: #fff;
  }
  .w-btn.danger {
    color: var(--danger);
  }
  .w-btn.small {
    padding: 5px 11px;
    font-size: 0.85rem;
  }
  .w-link {
    background: none;
    border: 0;
    padding: 0;
    font: inherit;
    color: var(--accent-2);
    cursor: pointer;
  }
  .icon-btn {
    background: none;
    border: 0;
    color: var(--dim);
    cursor: pointer;
    font-size: 1.05rem;
    padding: 4px 6px;
    border-radius: 8px;
  }
  .icon-btn:hover {
    color: #fff;
    background: var(--surface-2);
  }

  /* Sign in */
  .w-auth {
    max-width: 420px;
    margin: 40px auto;
  }
  .w-auth h1 {
    font-size: 1.9rem;
  }
  .w-auth form {
    display: grid;
    gap: 10px;
    margin-top: 12px;
  }
  .w-or {
    display: flex;
    align-items: center;
    gap: 10px;
    color: var(--dim);
    font-size: 0.85rem;
    margin: 18px 0 6px;
  }
  .w-or::before,
  .w-or::after {
    content: '';
    flex: 1;
    border-top: 1px solid var(--border);
  }
  .w-google {
    width: 100%;
    display: flex;
    gap: 10px;
    align-items: center;
    justify-content: center;
    background: #fff;
    color: #1f1f1f;
    border-color: #fff;
  }
  .w-err {
    color: var(--danger);
    font-size: 0.92rem;
    margin: 4px 0 0;
  }
  .w-note {
    font-size: 0.9rem;
    color: var(--dim);
  }

  /* Toolbar and lists */
  .w-bar {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    align-items: center;
    margin-bottom: 18px;
  }
  .w-bar h1 {
    font-size: 1.6rem;
    margin: 0 12px 0 0;
  }
  .chip {
    font: inherit;
    font-size: 0.88rem;
    font-weight: 700;
    border-radius: 999px;
    padding: 6px 12px;
    border: 1px solid var(--border);
    background: none;
    color: var(--dim);
    cursor: pointer;
  }
  .chip[aria-pressed='true'] {
    background: var(--accent-deep);
    border-color: var(--accent-2);
    color: #fff;
  }
  .w-group {
    margin-top: 26px;
  }
  .w-group > h2 {
    font-size: 1rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--dim);
    margin: 0 0 10px;
  }
  .w-jobs {
    display: grid;
    gap: 10px;
    grid-template-columns: repeat(auto-fill, minmax(290px, 1fr));
  }
  .job-card {
    position: relative;
    display: flex;
    gap: 12px;
    align-items: center;
    text-align: left;
    width: 100%;
    font: inherit;
    color: var(--text);
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 10px;
    cursor: pointer;
    overflow: hidden;
  }
  .job-card:hover {
    border-color: var(--accent-2);
  }
  .job-card.done .jc-title {
    text-decoration: line-through;
    color: var(--dim);
  }
  .jc-thumb {
    width: 64px;
    height: 64px;
    flex: none;
    border-radius: 12px;
    object-fit: cover;
    background: var(--surface-2);
    display: grid;
    place-items: center;
    color: var(--dim);
    font-size: 1.4rem;
  }
  .jc-body {
    min-width: 0;
    flex: 1;
  }
  .jc-title {
    font-weight: 800;
    color: var(--ink);
    display: block;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  .jc-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    margin-top: 5px;
    font-size: 0.8rem;
  }
  .pill {
    display: inline-flex;
    align-items: center;
    gap: 4px;
    border-radius: 999px;
    padding: 2px 8px;
    font-size: 0.78rem;
    font-weight: 700;
    border: 1px solid var(--border);
    color: var(--dim);
  }
  .pill.ok {
    color: #7ee2a2;
    border-color: rgba(63, 185, 107, 0.5);
    background: rgba(63, 185, 107, 0.1);
  }
  .pill.bad {
    color: #ff9c96;
    border-color: rgba(255, 90, 82, 0.5);
    background: rgba(255, 90, 82, 0.1);
  }
  .pill.warn {
    color: #ffd27a;
    border-color: rgba(242, 179, 61, 0.5);
    background: rgba(242, 179, 61, 0.1);
  }
  .pill.blue {
    color: #a9c8ff;
    border-color: rgba(79, 143, 240, 0.5);
    background: rgba(79, 143, 240, 0.12);
  }
  .w-empty {
    text-align: center;
    color: var(--dim);
    padding: 48px 16px;
    border: 1px dashed var(--border);
    border-radius: var(--radius);
  }

  .w-home {
    display: flex;
    align-items: center;
    gap: 14px;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 10px;
  }
  .w-home > div {
    display: flex;
    flex-direction: column;
    gap: 2px;
    min-width: 0;
  }
  .w-home strong {
    color: var(--ink);
    font-size: 1.1rem;
  }
  .w-home-img {
    width: 120px;
    height: 80px;
    object-fit: cover;
    border-radius: 12px;
    flex: none;
    cursor: zoom-in;
    background: var(--surface-2);
  }
  img[data-photo]:not([src]) {
    background: linear-gradient(100deg, var(--surface-2) 30%, #24405f 50%, var(--surface-2) 70%) 0 0 / 300% 100%;
    animation: shimmer 1.2s linear infinite;
  }
  img.missing-photo {
    animation: none;
    background: var(--surface-2);
  }
  @keyframes shimmer {
    to {
      background-position: -150% 0;
    }
  }

  /* Drawer */
  .w-scrim {
    position: fixed;
    inset: 0;
    background: rgba(5, 10, 16, 0.6);
    z-index: 40;
  }
  .w-drawer {
    position: fixed;
    top: 0;
    right: 0;
    bottom: 0;
    width: min(620px, 100%);
    background: var(--bg-2);
    border-left: 1px solid var(--border);
    z-index: 41;
    overflow-y: auto;
    overflow-x: hidden;
    box-shadow: -20px 0 60px rgba(0, 0, 0, 0.4);
  }
  .w-drawer-inner {
    padding: 18px 20px 60px;
  }
  .w-drawer-top {
    display: flex;
    align-items: center;
    gap: 8px;
    padding-right: 96px;
    margin-bottom: 10px;
  }
  .w-hero {
    width: 100%;
    aspect-ratio: 4 / 3;
    object-fit: cover;
    border-radius: var(--radius);
    border: 1px solid var(--border);
    background: var(--surface);
  }
  .w-photos {
    display: flex;
    gap: 8px;
    overflow-x: auto;
    margin-top: 8px;
  }
  .w-photos img {
    width: 84px;
    height: 84px;
    object-fit: cover;
    border-radius: 10px;
    cursor: zoom-in;
    flex: none;
  }
  .w-title-input {
    width: 100%;
    font-size: 1.5rem;
    font-weight: 800;
    background: none;
    border: 1px solid transparent;
    padding: 4px 6px;
    margin: 12px 0 4px -6px;
  }
  .w-title-input:hover {
    border-color: var(--border);
  }
  .w-section {
    margin-top: 24px;
  }
  .w-section h3 {
    font-size: 0.82rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--dim);
    margin: 0 0 10px;
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .seg {
    display: inline-flex;
    border: 1px solid var(--border);
    border-radius: 999px;
    padding: 3px;
    gap: 2px;
  }
  .seg button {
    font: inherit;
    font-size: 0.88rem;
    font-weight: 700;
    border: 0;
    border-radius: 999px;
    background: none;
    color: var(--dim);
    padding: 6px 12px;
    cursor: pointer;
  }
  .seg button[aria-pressed='true'] {
    background: var(--accent);
    color: #fff;
  }
  .w-row {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    align-items: center;
  }
  .w-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 6px;
  }
  .w-item {
    display: flex;
    align-items: center;
    gap: 10px;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 8px 10px;
  }
  .w-item .grow {
    flex: 1;
    min-width: 0;
  }
  .w-item .sub {
    display: block;
    font-size: 0.8rem;
    color: var(--dim);
  }
  .w-item.bought .name {
    text-decoration: line-through;
    color: var(--dim);
  }
  .check {
    width: 22px;
    height: 22px;
    flex: none;
    border-radius: 7px;
    border: 2px solid var(--dim);
    background: none;
    cursor: pointer;
    display: grid;
    place-items: center;
    color: #fff;
    font-size: 0.8rem;
    padding: 0;
  }
  .check.on {
    background: var(--ok);
    border-color: var(--ok);
  }
  .w-add {
    display: flex;
    gap: 8px;
    margin-top: 8px;
  }
  .w-add .w-input {
    flex: 1;
  }
  .w-steps {
    margin: 0;
    padding-left: 1.3em;
    display: grid;
    gap: 8px;
  }
  .w-warn {
    background: rgba(242, 179, 61, 0.08);
    border: 1px solid rgba(242, 179, 61, 0.35);
    border-radius: 12px;
    padding: 12px 14px;
  }
  .w-warn ul {
    margin: 0;
    padding-left: 1.2em;
  }
  .w-cost {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
  }
  .w-stat {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 10px 14px;
  }
  .w-stat strong {
    display: block;
    color: var(--ink);
    font-size: 1.1rem;
  }
  .w-stat span {
    font-size: 0.8rem;
    color: var(--dim);
  }

  /* Corner verdict sash, same shape as the app's */
  .sash {
    position: absolute;
    top: 0;
    right: 0;
    width: 132px;
    height: 132px;
    overflow: hidden;
    pointer-events: none;
    z-index: 2;
  }
  .sash span {
    position: absolute;
    top: 30px;
    right: -44px;
    width: 190px;
    transform: rotate(45deg);
    text-align: center;
    font-size: 0.7rem;
    font-weight: 900;
    letter-spacing: 0.08em;
    padding: 6px 0;
    color: #fff;
    box-shadow: 0 4px 14px rgba(0, 0, 0, 0.35);
  }
  .sash.diy span {
    background: #2e9d57;
  }
  .sash.tradie span {
    background: #d8342c;
  }
  .sash.none span {
    background: #3b4a5e;
    color: #c9d4e2;
  }
  .verdict-box {
    border-radius: 12px;
    padding: 12px 14px;
    border: 1px solid var(--border);
    background: var(--surface);
  }
  .verdict-box.diy {
    border-color: rgba(63, 185, 107, 0.5);
  }
  .verdict-box.tradie {
    border-color: rgba(255, 90, 82, 0.55);
  }

  /* Shopping */
  .w-cols {
    display: grid;
    gap: 22px;
  }
  @media (min-width: 900px) {
    .w-cols {
      grid-template-columns: 1fr 1fr;
    }
  }
  .w-retailer h2 {
    font-size: 1.05rem;
    margin: 0 0 8px;
  }
  details.w-bought summary {
    cursor: pointer;
    color: var(--dim);
    font-weight: 700;
    margin: 18px 0 8px;
  }

  /* Gallery */
  .w-gallery {
    display: grid;
    gap: 8px;
    grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
  }
  .w-gallery button {
    padding: 0;
    border: 0;
    background: var(--surface);
    border-radius: 12px;
    overflow: hidden;
    cursor: zoom-in;
    position: relative;
    aspect-ratio: 1;
  }
  .w-gallery img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }
  .w-gallery .cap {
    position: absolute;
    left: 0;
    right: 0;
    bottom: 0;
    padding: 18px 8px 6px;
    font-size: 0.75rem;
    color: #fff;
    text-align: left;
    background: linear-gradient(transparent, rgba(0, 0, 0, 0.75));
  }
  .w-lightbox {
    position: fixed;
    inset: 0;
    z-index: 60;
    background: rgba(0, 0, 0, 0.92);
    display: grid;
    place-items: center;
    padding: 20px;
    cursor: zoom-out;
  }
  .w-lightbox img {
    max-width: 100%;
    max-height: 92vh;
    border-radius: 10px;
  }

  /* Account */
  .w-account {
    display: grid;
    gap: 14px;
    max-width: 620px;
  }
  .w-account .card p {
    margin: 4px 0 0;
  }

  .w-toast {
    position: fixed;
    left: 50%;
    bottom: 24px;
    transform: translate(-50%, 20px);
    background: var(--surface-2);
    color: #fff;
    border: 1px solid var(--border);
    border-radius: 999px;
    padding: 10px 18px;
    font-weight: 700;
    opacity: 0;
    pointer-events: none;
    transition: all 0.2s ease;
    z-index: 70;
    max-width: calc(100% - 32px);
  }
  .w-toast.show {
    opacity: 1;
    transform: translate(-50%, 0);
  }
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    overflow: hidden;
    clip: rect(0 0 0 0);
  }
  @media (max-width: 760px) {
    .w-top {
      flex-wrap: wrap;
      gap: 8px 12px;
    }
    .w-site {
      order: 3;
      flex-basis: 100%;
    }
    .w-top-slot {
      justify-content: flex-end;
    }
    .hide-sm {
      display: none !important;
    }
  }

  /* ===== App parity (v2) ===== */
  .card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 16px;
  }
  .faint { color: var(--dim); font-size: 0.9rem; margin: 4px 0; }
  .count { font-size: 0.8rem; color: var(--dim); font-weight: 700; margin-left: 4px; }
  .w-chips { display: flex; flex-wrap: wrap; gap: 6px; }
  .chip.ghost { border-style: dashed; }
  .chip-radio input { position: absolute; opacity: 0; pointer-events: none; }
  .chip-radio span { display: inline-block; font-size: 0.88rem; font-weight: 700; border-radius: 999px; padding: 6px 12px; border: 1px solid var(--border); color: var(--dim); cursor: pointer; }
  .chip-radio input:checked + span { background: var(--accent-deep); border-color: var(--accent-2); color: #fff; }
  .chip-radio input:focus-visible + span { outline: 2px solid var(--accent-2); }
  .stack { display: grid; gap: 14px; }
  .stack label { display: grid; gap: 6px; font-weight: 700; color: var(--ink); font-size: 0.92rem; }
  fieldset.bare { border: 0; padding: 0; margin: 0; }
  fieldset.bare legend { font-weight: 700; color: var(--ink); font-size: 0.92rem; margin-bottom: 6px; }
  .w-input.full { width: 100%; }
  .w-input.narrow { max-width: 150px; }
  .w-input.small { padding: 4px 8px; font-size: 0.85rem; }
  .w-link.small { font-size: 0.85rem; }
  .w-btn:disabled { opacity: 0.45; cursor: not-allowed; }

  /* Home header */
  .home-head { text-align: center; }
  .houses { display: flex; gap: 6px; justify-content: center; flex-wrap: wrap; margin-bottom: 10px; }
  .house-name { background: none; border: 0; color: #fff; font: inherit; font-size: 1.9rem; font-weight: 800; cursor: pointer; }
  .house-name span { font-size: 1.1rem; color: var(--dim); }
  .house-facts { margin: 2px 0 0; color: #fff; font-weight: 700; font-size: 0.95rem; }
  .summary { max-width: 640px; margin: 10px auto 0; }
  .summary textarea { text-align: center; }

  /* Spaces slider, mirrors the app's SpaceTabBar */
  .space-bar { display: flex; gap: 22px; overflow-x: auto; scrollbar-width: thin; margin: 18px 0 14px; padding: 0 2px; border-bottom: 1px solid var(--border); }
  .space-tab { position: relative; flex: none; background: none; border: 0; font: inherit; font-size: 1.02rem; font-weight: 500; color: var(--dim); padding: 8px 0 10px; border-bottom: 2.5px solid transparent; cursor: pointer; white-space: nowrap; display: inline-flex; align-items: center; gap: 6px; }
  .space-tab:hover { color: #fff; }
  .space-tab[aria-current='true'] { color: #fff; font-weight: 800; border-bottom-color: var(--accent-2); }
  .space-more { font-size: 1.1rem; line-height: 1; padding: 0 4px; border-radius: 6px; color: var(--dim); }
  .space-more:hover { background: var(--surface-2); color: #fff; }
  .space-banner { position: relative; display: block; width: 100%; height: 170px; border-radius: var(--radius); overflow: hidden; margin-bottom: 14px; border: 1px solid var(--border); }
  .space-banner img { width: 100%; height: 100%; object-fit: cover; display: block; }
  .space-banner span { position: absolute; left: 16px; bottom: 12px; color: #fff; font-weight: 800; font-size: 1.3rem; text-shadow: 0 2px 10px rgba(0, 0, 0, 0.7); }
  .space-banner.empty { background: var(--surface); color: var(--dim); font: inherit; font-weight: 700; cursor: pointer; border-style: dashed; height: 64px; }
  .swipe { touch-action: pan-y; }

  /* Job groups and tiles, mirrors the app's _JobTile */
  .group { margin-bottom: 14px; padding: 13px; }
  .group h2, .group-toggle { display: flex; align-items: center; gap: 6px; margin: 0 0 8px; font-size: 1.05rem; color: #fff; }
  .group-toggle { background: none; border: 0; font: inherit; font-weight: 700; cursor: pointer; width: 100%; padding: 0; }
  .group-toggle .chev { margin-left: auto; }
  .gi { color: var(--accent-2); }
  .tile { background: rgba(255, 255, 255, 0.05); border: 1px solid var(--border); border-radius: 10px; margin-bottom: 5px; }
  .tile-row { display: flex; align-items: center; }
  .tick { flex: none; width: 19px; height: 19px; margin-left: 10px; border-radius: 5px; border: 1.5px solid rgba(255, 255, 255, 0.55); background: none; color: #fff; font-size: 0.72rem; cursor: pointer; display: grid; place-items: center; padding: 0; }
  .tick.on { background: var(--accent); border-color: var(--accent); }
  .tile-main { flex: 1; min-width: 0; display: flex; align-items: center; gap: 6px; background: none; border: 0; color: #fff; font: inherit; text-align: left; padding: 9px 10px; cursor: pointer; }
  .tile-title { font-weight: 600; font-size: 0.97rem; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; min-width: 0; }
  .tile.open .tile-title { white-space: normal; }
  .tile.done .tile-title { color: var(--dim); text-decoration: line-through; }
  .ico { color: var(--accent-2); font-size: 0.85rem; }
  .ico.spark { color: #c69bff; }
  .tile-meta { color: var(--dim); font-size: 0.78rem; flex: none; }
  .chev { color: var(--dim); flex: none; }
  .tile-body { border-top: 1px solid var(--border); padding: 8px 12px 12px; font-size: 0.88rem; }
  .tile-photos { display: flex; gap: 6px; overflow-x: auto; margin-bottom: 8px; }
  .tile-photos img { height: 120px; width: 160px; object-fit: cover; border-radius: 8px; cursor: zoom-in; flex: none; background: var(--surface-2); }
  .tile-line { margin: 3px 0; color: var(--dim); }
  .tile-line.strong { color: #fff; font-weight: 700; }
  .tile-desc { color: #fff; margin: 6px 0; }
  .tile-h { margin: 8px 0 2px; font-weight: 700; color: #fff; }
  .tile-list { margin: 0; padding-left: 1.3em; color: var(--dim); }
  .tile-list .struck { text-decoration: line-through; }
  .tile-open { display: block; margin: 10px 0 0 auto; font-weight: 700; font-style: italic; }

  /* Job drawer extras */
  .jp-row { display: flex; gap: 8px; overflow-x: auto; padding: 4px 0 8px; }
  .jp { position: relative; margin: 0; flex: none; }
  .jp img, .jp-row img.mini { width: 110px; height: 110px; object-fit: cover; border-radius: 12px; cursor: zoom-in; display: block; background: var(--surface-2); }
  .jp figcaption { position: absolute; left: 6px; bottom: 6px; font-size: 0.7rem; font-weight: 800; color: #fff; background: rgba(0, 0, 0, 0.55); border-radius: 999px; padding: 1px 7px; }
  .jp-x { position: absolute; top: 4px; right: 4px; width: 22px; height: 22px; border-radius: 50%; border: 0; background: rgba(0, 0, 0, 0.6); color: #fff; font-size: 0.7rem; cursor: pointer; }
  .jp-add { flex: none; width: 110px; height: 110px; border-radius: 12px; border: 1.5px dashed var(--border); background: var(--surface); color: var(--dim); font: inherit; font-size: 1.6rem; cursor: pointer; display: grid; place-content: center; gap: 2px; }
  .jp-add span { font-size: 0.75rem; font-weight: 700; }
  .done-btn { font: inherit; font-weight: 800; border-radius: 999px; padding: 8px 16px; border: 1.5px solid var(--ok); background: none; color: #7ee2a2; cursor: pointer; }
  .done-btn.on { background: var(--ok); color: #fff; }
  .verdict-box p { margin: 6px 0 0; }
  .steps-edit { margin: 0; padding-left: 1.4em; display: grid; gap: 8px; }
  .steps-edit li { display: flex; gap: 6px; align-items: flex-start; }
  .steps-edit textarea.step { min-height: 44px; flex: 1; }
  .step-btns { display: flex; flex-direction: column; }
  .thread { display: grid; gap: 8px; }
  .msg { padding: 10px 12px; border-radius: 12px; font-size: 0.9rem; white-space: pre-wrap; max-width: 88%; }
  .msg.me { background: var(--accent-deep); justify-self: end; }
  .msg.ai { background: var(--surface); border: 1px solid var(--border); }

  /* Modal */
  .w-scrim.top { z-index: 50; }
  .w-modal { position: fixed; z-index: 51; left: 50%; top: 50%; transform: translate(-50%, -50%); width: min(440px, calc(100% - 32px)); max-height: calc(100vh - 40px); overflow-y: auto; background: var(--bg-2); border: 1px solid var(--border); border-radius: var(--radius); padding: 22px; box-shadow: 0 30px 80px rgba(0, 0, 0, 0.5); }
  .w-modal h2 { margin: 0 0 14px; font-size: 1.2rem; padding-right: 30px; }
  .modal-x { position: absolute; top: 12px; right: 12px; }
  .menu { display: grid; gap: 6px; }
  .menu button { text-align: left; font: inherit; font-weight: 600; background: var(--surface); border: 1px solid var(--border); color: #fff; padding: 12px 14px; border-radius: 12px; cursor: pointer; }
  .menu button:hover { border-color: var(--accent-2); }
  .menu button.danger { color: var(--danger); }
  .w-uploading { position: fixed; z-index: 70; left: 50%; bottom: 80px; transform: translateX(-50%); display: flex; gap: 10px; align-items: center; background: var(--surface-2); border: 1px solid var(--border); padding: 10px 18px; border-radius: 999px; font-weight: 700; }

  /* Gallery */
  .sel-bar { display: flex; flex-wrap: wrap; gap: 6px; align-items: center; background: var(--surface); border: 1px solid var(--accent-2); border-radius: 12px; padding: 8px 10px; margin-bottom: 12px; position: sticky; top: 110px; z-index: 5; }
  .g-tile { position: relative; padding: 0; border: 2px solid transparent; background: var(--surface); border-radius: 12px; overflow: hidden; cursor: pointer; aspect-ratio: 1; }
  .g-tile.on { border-color: var(--accent-2); }
  .g-tile > img { width: 100%; height: 100%; object-fit: cover; display: block; }
  .g-tile .cap { position: absolute; left: 0; right: 0; bottom: 0; padding: 18px 8px 6px; font-size: 0.75rem; color: #fff; text-align: left; background: linear-gradient(transparent, rgba(0, 0, 0, 0.75)); }
  .g-check { position: absolute; top: 8px; right: 8px; width: 24px; height: 24px; border-radius: 50%; border: 2px solid #fff; background: rgba(0, 0, 0, 0.35); color: #fff; display: grid; place-items: center; font-size: 0.8rem; }
  .g-tile.on .g-check { background: var(--accent); border-color: var(--accent); }
  .pair { position: absolute; inset: 0; }
  .pair img { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; }
  .pair .pa { clip-path: polygon(100% 0, 100% 100%, 0 100%); }
  .pair-tag { position: absolute; top: 8px; left: 8px; z-index: 1; font-size: 0.7rem; font-weight: 800; background: rgba(0, 0, 0, 0.6); color: #fff; padding: 2px 8px; border-radius: 999px; }
  .timeline { position: relative; max-width: 760px; margin: 0 auto; padding: 8px 0 20px; }
  .timeline::before { content: ""; position: absolute; left: 50%; top: 0; bottom: 0; width: 2px; background: var(--border); }
  .season { position: relative; text-align: center; background: var(--bg); width: max-content; margin: 18px auto 10px; padding: 4px 12px; border-radius: 999px; border: 1px solid var(--border); font-size: 0.9rem; }
  .tl-row { position: relative; display: flex; gap: 10px; align-items: center; width: calc(50% - 20px); margin: 8px 0; background: var(--surface); border: 1px solid var(--border); border-radius: 12px; padding: 8px; font: inherit; color: var(--text); text-align: left; cursor: pointer; }
  .tl-row:hover { border-color: var(--accent-2); }
  .tl-row.right { margin-left: calc(50% + 20px); }
  .tl-row .sub { display: block; font-size: 0.8rem; color: var(--dim); }
  .tl-img { width: 64px; height: 64px; border-radius: 10px; object-fit: cover; flex: none; background: var(--surface-2); }
  .tl-img.blank { display: grid; place-items: center; color: var(--ok); }
  .tl-end { position: relative; text-align: center; font-size: 1.4rem; color: var(--dim); }
  @media (max-width: 640px) {
    .timeline::before { left: 12px; }
    .tl-row, .tl-row.right { width: calc(100% - 30px); margin-left: 30px; }
  }

  /* Before/after comparison slider */
  .compare-wrap { cursor: default; }
  .lb-x { position: absolute; top: 16px; right: 16px; color: #fff; font-size: 1.4rem; z-index: 2; }
  .compare { position: relative; width: min(1000px, 94vw); aspect-ratio: 4 / 3; max-height: 86vh; border-radius: 12px; overflow: hidden; background: #111; }
  .compare img { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; max-width: none; max-height: none; border-radius: 0; }
  .cmp-after { position: absolute; inset: 0; clip-path: inset(0 0 0 var(--pos)); }
  .compare::after { content: ""; position: absolute; top: 0; bottom: 0; left: var(--pos); width: 3px; background: #fff; transform: translateX(-50%); pointer-events: none; }
  .cmp-label { position: absolute; top: 12px; font-weight: 800; font-size: 0.8rem; background: rgba(0, 0, 0, 0.6); color: #fff; padding: 3px 10px; border-radius: 999px; z-index: 1; }
  .cmp-label.l { left: 12px; }
  .cmp-label.r { right: 12px; }
  .cmp-range { position: absolute; inset: 0; width: 100%; height: 100%; opacity: 0; cursor: ew-resize; margin: 0; z-index: 2; }

  /* Settings */
  .settings { display: grid; gap: 14px; max-width: 760px; }
  .settings h2 { margin: 0 0 10px; font-size: 1.1rem; }
  .set-grid { display: grid; gap: 10px; grid-template-columns: repeat(auto-fill, minmax(200px, 1fr)); margin: 12px 0; }
  .set-field { display: grid; gap: 4px; font-size: 0.85rem; color: var(--dim); font-weight: 700; }
  .set-field.full { margin: 10px 0; }
  .house-photo { display: flex; align-items: center; gap: 12px; }
  .house-photo img { width: 160px; height: 100px; object-fit: cover; border-radius: 12px; }
  .house-photo .blank, .room-thumb.blank { display: grid; place-items: center; color: var(--dim); background: var(--surface-2); border-radius: 10px; }
  .house-photo .blank { width: 160px; height: 100px; font-size: 0.85rem; }
  .room-thumb { width: 44px; height: 44px; border-radius: 10px; object-fit: cover; flex: none; }
  .w-item .w-input.grow { flex: 1; min-width: 0; }

  @media (max-width: 640px) {
    .w-tabs button { padding: 8px 9px; font-size: 0.9rem; }
    .house-name { font-size: 1.5rem; }
    .space-bar { gap: 16px; }
    .tile-photos img { width: 130px; height: 100px; }
  }
</style>
